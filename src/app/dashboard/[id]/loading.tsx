import Loading from "@/components/Loading/Loading";
import { FC } from "react";

const DashboardPageLoading: FC = () => {
    return (
        <main className="my-4">
            <Loading />
        </main>
    );
};

export default DashboardPageLoading;
