import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'dark'
    },
    typography: {
        fontFamily: 'Nunito'
    }
});

export const themeColor = '#007bff';